plugins {
    id 'com.android.application'
    // block:start:apply-plugin
    id 'hypersdk.plugin'
    // block:end:apply-plugin
}

android {
    namespace "in.juspay.devtools"
    compileSdk 34

    defaultConfig {
        applicationId "in.juspay.devtools"
        minSdk 21
        targetSdk 31
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    // Aligns kotlin-stdlib / -jdk7 / -jdk8 to one version. Kotlin 1.8 merged the
    // jdk7/jdk8 artifacts into kotlin-stdlib; the BOM makes the old jdk7/jdk8
    // coords resolve to empty 1.8.22 stubs, avoiding duplicate-class errors.
    implementation platform('org.jetbrains.kotlin:kotlin-bom:1.8.22')
    implementation 'androidx.appcompat:appcompat:1.4.1'
    implementation 'com.google.android.material:material:1.5.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.3'
    implementation 'com.google.android.material:material:1.1.0'
    implementation("com.squareup.okhttp3:okhttp:4.9.3")
    testImplementation 'junit:junit:4.+'
    androidTestImplementation 'androidx.test.ext:junit:1.1.3'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.4.0'
}

//configurations.all {
//    resolutionStrategy {
//        force "org.jetbrains.kotlin:kotlin-stdlib:1.8.22"
//        force "org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.8.22"
//        }
//}

//block:start:add-sdk-implementation
hyperSdkPlugin {
    clientId = "hdfcmaster"
    sdkVersion = "2.2.8-rc.04"
}
//block:end:add-sdk-implementation

