# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

use_frameworks!


target 'ObjectiveC' do
  # Adding BharatPeX Payment SDK dependecy
  # block:start:adding-bharatpexpayment-sdk-pod
  pod 'BharatPeXPaymentSDK', '2.1.31'
  # block:end:adding-bharatpexpayment-sdk-pod
end


# Adding BharatPeXPayment SDK dependecy
# block:start:adding-plugin-installation-script
post_install do |installer|
 fuse_path = "./Pods/BharatPeXPaymentSDK/Fuse.rb"
 clean_assets = false # Pass true to re-download all the assets
 if File.exist?(fuse_path)
   if system("ruby", fuse_path.to_s, clean_assets.to_s)
   end
 end
end
# block:end:adding-plugin-installation-script
